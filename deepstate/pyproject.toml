[project]
name = "deepstate"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "geopandas",
    "ipykernel",
    "jupyter",
    "matplotlib",
    "pandas",
    "pymongo",
    "python-dotenv",
    "shapely",
]
